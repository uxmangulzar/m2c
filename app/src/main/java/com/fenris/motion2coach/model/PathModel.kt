package com.fenris.motion2coach.model

import android.graphics.Paint
import android.graphics.Path

data class PathModel(val path: Path?=Path(),
                     val paint: Paint?= Paint()
                        )
